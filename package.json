{
  "name": "multicast-ipc",
  "version": "1.0.0",
  "description": "A promise-based IPC implementation that uses dgram multicast under the hood.",
  "main": "index.js",
  "scripts": {
    "docs": "jsdoc2md \"src/**/*.js\" --separators --sort-by name --template ./tutorials/README_tpl.hbs > README.md",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Anton Vishnyak",
  "license": "ISC",
  "dependencies": {
    "bluebird": "^3.4.0"
  },
  "devDependencies": {
    "jsdoc-to-markdown": "^1.3.6"
  }
}
